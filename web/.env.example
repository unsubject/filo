# Copy to `.env.local` and fill in. Both are optional in dev.

# Base URL of the filo Worker API. Defaults to http://localhost:8787.
VITE_API_BASE=http://localhost:8787

# Single-user bearer token. If unset here, the client falls back to
# localStorage.getItem("filo_token"). Never commit a real token.
VITE_FILO_TOKEN=
